<form method="post" action="{{ path('suggested_tasks_delete', {'id': suggested_task.id}) }}" onsubmit="return confirm('Are you sure you want to delete this item?');">
    <input type="hidden" name="_token" value="{{ csrf_token('delete' ~ suggested_task.id) }}">
    <button class="btn btn-outline-danger" style="margin-left: 10px;">Delete</button>
</form>
