{% extends 'base.html.twig' %}

{% block title %}MyNotes{% endblock %}

{% block body %}
<div class="d-flex justify-content-start align-content-center">
    <a href="{{ path('my_notes_index') }}" style="margin: 5px 0 0 5px;"><svg xmlns="http://www.w3.org/2000/svg" width="28" height="28" fill="currentColor" class="bi bi-caret-left-square-fill" viewBox="0 0 16 16">
        <path d="M0 2a2 2 0 0 1 2-2h12a2 2 0 0 1 2 2v12a2 2 0 0 1-2 2H2a2 2 0 0 1-2-2zm10.5 10V4a.5.5 0 0 0-.832-.374l-4.5 4a.5.5 0 0 0 0 .748l4.5 4A.5.5 0 0 0 10.5 12"/></svg>
    </a>
</div>
    <h1>MyNotes</h1>

    <table class="table">
        <tbody>
            <tr>
                <th>Id</th>
                <td>{{ my_note.id }}</td>
            </tr>
            <tr>
                <th>Title</th>
                <td>{{ my_note.title }}</td>
            </tr>
            <tr>
                <th>Note</th>
                <td>{{ my_note.description }}</td>
            </tr>
            <tr>
                <th>Created_at</th>
                <td>{{ my_note.createdAt ? my_note.createdAt|date('Y-m-d H:i:s') : '' }}</td>
            </tr>
        </tbody>
    </table>


    <a href="{{ path('my_notes_edit', {'id': my_note.id}) }}">edit</a>

    {{ include('my_notes/_delete_form.html.twig') }}
{% endblock %}
